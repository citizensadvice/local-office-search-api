default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  username: <%= ENV['SEARCH_DB_USER'] %>
  password: <%= ENV['SEARCH_DB_PASSWORD'] %>
  host: <%= ENV['SEARCH_DB_HOST'] %>
  port: <%= ENV.fetch('SEARCH_DB_PORT', 5432).to_i %>
  database: <%= ENV['SEARCH_DB_NAME'] %>
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: search-api_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: search-api_test

production:
  <<: *default
